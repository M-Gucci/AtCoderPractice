#include<iostream>
#include<vector>

int main(){
    int n,k;
    std::cin >> n >> k;
    std::vector<int> jug;
    for(int i=0;i<n;i++){
        int tmp;
        std::cin >> tmp;
        jug.push_back(tmp);
    }

    
}