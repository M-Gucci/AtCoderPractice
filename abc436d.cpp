#include<iostream>
#include<string>
#include<algorithm>
#include<string>
#include<utility>
#include<map>
#include<vector>

int main(){
    int h, w;
    std::vector<std::string> s;
    for(int i=0;i<h;i++){
        std::string ts;
        std::cin >> ts;
        s.push_back(ts);
    }

    
}